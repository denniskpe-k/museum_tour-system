No real 3D model files are bundled with this project (no budget for
3D scanning equipment or asset purchases). ModelGuide content objects
reference file paths in this folder as metadata only, to demonstrate
the data model and UI flow for 3D exhibits. A real deployment would
place actual .glb/.obj files here and hand them to a 3D viewer widget.
