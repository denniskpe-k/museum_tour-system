Real video files go here.

Filenames the app expects are listed in MEDIA_GUIDE.md at the project
root. Any standard .mp4 (H.264) file will work — the Media popup's
Video tab plays it directly. If a file isn't present yet, that stop's
Video tab shows a friendly "not added yet" message instead of an
error, so it's safe to add these one at a time.
